# hersona CLI message catalog — English (BASE / default language)
#
# Referenced by dotted key (core/i18n.tr). Keep this file and ja.yaml in sync;
# keys missing from a non-default catalog fall back to this file, then to the
# key string itself.

error:
  prefix: "Error: "
  memory_mutually_exclusive: "Error: --memory and --memory-file cannot be used together"

common:
  yes: "yes"
  no: "no"
  none: "(none)"

cli:
  description: "hersona attribute template CLI"
  lang_help: "Output language (default: en, or the HERSONA_LANG env var)"
  plain_help: "Disable rich tables/panels even when rich is installed"

help:
  list: "List available attributes"
  show: "Show attribute details"
  show_name: "Attribute name (e.g. tsundere) or <category>/<name>"
  matrix: "Dump the compatibility matrix"
  json: "Emit machine-readable JSON"
  blend: "Blend attributes into a prompt injection block"
  names: "Attribute name(s)"
  weight_blend: "Intensity (default: moderate)"
  recommend: "Diagnostic quiz -> recommendation"
  rec_answers: "Non-interactive: answers as 'distance=1,role=1'"
  rec_apply: "Also show the injection block for the recommended blend"
  rec_weight: "Intensity for --apply (default: inferred from fit score)"
  rec_explain: "Show rationale per adopted attribute / alternatives / summary"
  rec_top: "Number of candidate blends to return (default: 1)"
  rec_quiz_mode: "Quiz mode: 'v1' (linear, default) or 'v2' (decision tree)"
  rec_generate_samples: "Generate sample dialogue lines for each candidate (default: off)"
  rec_sample_count: "Number of sample lines per candidate when --generate-samples is set (default: 3)"
  rec_export: "Export the recommended blend directly (no need to re-enter names into 'hersona export')"
  rec_output: "Write the --export payload to a file instead of stdout"
  rec_soul: "Write the recommended blend straight to SOUL.md (~/.hermes/profiles/<profile>/SOUL.md)"
  rec_soul_output: "Explicit SOUL.md output path for --soul (default: profile path)"
  rec_force: "Allow --soul to overwrite an existing SOUL.md"
  rec_dry_run: "With --soul: print the SOUL.md content to stdout instead of writing"
  rec_save: "Save the recommended blend as a named preset (reload later with 'hersona load')"
  rec_install_persona: "Register the recommended blend as a persona pack under <name> in agent.personalities (PR-B W1 §5)"
  preview: "Show injection block + sample phrases for a blend (no LLM required)"
  preview_count: "Number of sample phrases to show (default: 3)"
  diff: "Compare two attributes (common / only-one fields + relation)"
  diff_name: "Attribute name (e.g. tsundere) or <category>/<name>"
  suggest: "On conflict, suggest non-conflicting replacement attributes (to stderr)"
  create: "Create an attribute and save it"
  create_example: "Repeatable"
  measure: "Score intensity metrics of output text (blends without a speech attribute are skipped)"
  weight_measure: "Expected band (default: moderate)"
  measure_input: "Path to the text file to score"
  measure_text: "Text to score (inline)"
  measure_strict: "When status is under/over, also emit a pasteable self-audit prompt"
  measure_check_prompt: "Print only the self-audit prompt (suppress the score report)"
  bench: "Score a conversation transcript for persona-maintenance rate and injection token cost (see docs/BENCHMARKS.md)"
  bench_transcript: "Path to a JSON file: a list of assistant-response strings (one per turn)"
  bench_scenario: "Path to a benchmarks/scenarios/*.yaml file (supplies the report's scenario label and, for attack scenarios, the attack-turn markers used for the lock resistance rate)"
  bench_demo: "Score a self-contained synthetic transcript instead of --transcript (no LLM calls; smoke-test only, not a quality benchmark)"
  bench_turns: "Number of turns to generate with --demo (default: 6)"
  bench_cost_only: "Only report the injection block's char count / approx token estimate; skip transcript scoring"
  lint_intro: "Lint public self-introduction text (deterministic rules; see docs/guides/self-introduction.md)"
  lint_intro_input: "Path to UTF-8 text file to lint"
  lint_intro_text: "Intro text to lint (inline)"
  lint_intro_allow_handle: "Allowed @handle without @ prefix (repeatable; e.g. hersona_agent)"
  lint_intro_canonical: "Stricter rules for SOUL self_intro_canonical (e.g. no マスター)"
  lint_intro_json: "Emit JSON result"
  save: "Save a blend as a reusable named preset (local)"
  save_name: "Preset name (snake_case)"
  save_note: "Optional note describing the preset"
  save_overwrite: "Overwrite an existing preset of the same name"
  presets: "List saved blend presets"
  load: "Replay a saved preset as an injection block"
  load_name: "Preset name to load"
  load_weight: "Override the saved intensity on replay"
  export: "Export a blend for other agent frameworks (LangGraph / OpenAI / ...)"
  export_format: "Output format: json (default) / messages / markdown / openai_assistants / langchain_system_message"
  soul: "Write a blend to ~/.hermes/profiles/<name>/SOUL.md (Hermes One official spec)"
  soul_profile: "Target profile name (default: default)"
  soul_output: "Output path (default: ~/.hermes/profiles/<profile>/SOUL.md)"
  soul_name: "Display name in SOUL.md Name section (default: Libra)"
  soul_append: "Append a '## 追記' section to an existing SOUL.md"
  soul_overwrite: "Allow overwriting an existing SOUL.md"
  soul_force: "Overwrite without confirmation (CI / scripts)"
  soul_dry_run: "Print the SOUL.md content to stdout without writing"
  soul_memory: "Caller-supplied memory as JSON object (max 16 keys, 512 chars/value)"
  soul_memory_file: "Path to a UTF-8 JSON file with caller-supplied memory"
  with_self_intro_guide: "Merge default self_intro_style and privacy_inner_circle from docs/guides"
  lint_self_intro_on_memory: "Lint self_intro_canonical in memory before writing SOUL (warn on fail)"
  lint_self_intro_strict: "With lint: abort write on violations (exit 1)"
  persistent: "Persist a blend to SOUL.md and print a config.yaml block to paste (ROADMAP §⑤.1)"
  persistent_memory: "Caller-supplied memory as JSON object (max 16 keys, 512 chars/value)"
  persistent_memory_file: "Path to a UTF-8 JSON file with caller-supplied memory"
  persistent_force: "Force-overwrite an existing SOUL.md"
  persistent_without_soul: "Skip SOUL.md write (only show config.yaml block)"
  persistent_without_config: "Skip config.yaml block (only write SOUL.md)"
  persistent_yaml_output: "Write the YAML block to a file (not auto-applied to config.yaml)"
  persistent_auto_config: "Automatically write the personality entry to config.yaml (PyYAML direct edit, no hermes config set)"
  personas: "List / show / install / use persona packs (PR-B W1)"
  personas_list: "List available persona packs"
  personas_show: "Show a persona pack recipe + rendered preview"
  personas_install: "Install one or more persona packs into the local Hermes profile"
  personas_install_name: "Persona-pack ID(s) (1+ required)"
  personas_install_profile: "Target Hermes profile"
  personas_install_auto_config: "Write into agent.personalities"
  personas_install_apply: "hermes config set agent.personality <name> (only the last pack)"
  personas_install_with_soul: "Also write SOUL.md (default: skip)"
  personas_install_force: "Overwrite existing entries"
  personas_install_dry_run: "Print the YAML block(s) instead of writing"
  personas_use: "Switch agent.personality to <name> (hermes config set)"
  personas_use_name: "Persona-pack ID to activate"
  personas_not_found: "Persona pack not found: {name}"
  personas_validation_failed: "Persona pack '{name}' failed validation: {errors}"
  personas_installed: "✓ installed: {name} (persona_name={persona_name})"
  personas_apply: "apply {name}: {result}"
  personas_bulk_warning: "⚠ Installing {count} packs at once; estimated total injection block size ≈ {chars} chars. config.yaml will grow accordingly."
  personas_bulk_failed: "{failed}/{total} pack(s) failed."
  personas_pack_line: "  - {name}: weight={weight} use_case={use_case} blend={blend}attrs"
  persistent_config_path: "Override config.yaml path (default: ~/.hermes/config.yaml)"
  persistent_apply: "Run 'hermes config set agent.personality <name>' to activate immediately (requires hermes CLI)"
  persistent_target: "Persistence target: hermes (default) / claude / codex / cursor / gemini"
  persistent_global: "Write to the global location (e.g. ~/.claude/CLAUDE.md) instead of the project dir"
  persistent_output: "Override the target file path (for non-hermes targets)"
  update: "Download the latest attribute data from the repository"
  update_ref: "Git ref to fetch: branch / tag / commit SHA (default: main)"
  update_dry_run: "Show the source URL and destination without downloading"
  update_clear: "Remove downloaded data and revert to the bundled templates"
  update_no_verify: "Skip checksums.json verification of the downloaded data (see docs/SECURITY.md)"

list:
  header: "Available attributes ({count}):"
  category: "{category}/ ({count})"
  user_tag: " [user]"
  col_category: "category"
  col_attribute: "attribute"
  col_source: "source"

summary:
  speech_personality_archetype: "a {personality} {archetype} who speaks with {speech}"
  speech_personality: "a {personality} who speaks with {speech}"
  speech_archetype: "a {archetype} who speaks with {speech}"
  tail_join: ", "
  sentence_join: ". "

recommend:
  header: "=== Recommendation ==="
  blend: "Blend: {blend}"
  summary: "Summary: {summary}"
  weight: "Suggested intensity: {weight}"
  top: "Top fit: {items}"
  dropped: "  dropped: {name} — {reason}"
  rationale_header: "--- rationale ---"
  rationale_item: "question \"{prompt}\" -> \"{label}\""
  conflict_reason: "conflicts with {names}"
  alt_header: "--- alternatives (dropped -> suggested) ---"
  alt_item: "  {dropped} -> {alt} (score {score})"
  inject_header: "--- injection block (intensity: {weight}) ---"
  baseline_recorded: "intensity baseline: {score}/100 (band {lo}-{hi} {weight})"
  json_exclusive: "--json cannot be combined with --export / --soul / --save"
  empty_blend: "no attributes were recommended from these answers — nothing to export/save"
  export_written: "export ({fmt}) written: {path}"
  soul_exists: "SOUL.md already exists: {path} — pass --force to overwrite (or use 'hersona soul --append')"
  preset_note: "saved from 'hersona recommend --save'"
  install_persona_ok: "✓ registered persona pack: {name}"
  config_written: "  config.yaml: {path}"
  apply_result: "  apply: {result}"

quiz:
  prompt_select: "Select (number): "
  invalid_number: "  * please enter a valid number"

create:
  saved: "Saved: {dest}"
  namespace: "(user namespace: {root})"
  missing_flags: "missing required flags: {flags}"
  wizard_header: "=== Attribute creation wizard ==="
  ask_name: "attribute_name (snake_case): "
  ask_display_ja: "Display name (Japanese): "
  ask_display_en: "Display name (English): "
  ask_desc_ja: "Description (Japanese): "
  ask_desc_en: "Description (English): "
  ask_examples: "Enter examples one per line (blank to finish):"
  ask_example: "  example: "
  label_category: "category"

prompt:
  choice: "{label} {choices}: "
  invalid_choice: "  * please choose from {choices}"

blend:
  conflict: "⚠ conflict: {conflicts}"

suggest:
  header: "suggestions to resolve conflicts:"
  item: "  {a} ⇔ {b}: drop {drop} → try: {alts}"

diff:
  header: "=== diff: {a} vs {b} ==="
  category: "category: {a} | {b}"
  relation: "relation: {relation}"
  relation_unknown: "n/a (not in public matrix)"
  scalar_a: "  {name}: {value}"
  scalar_b: "  {name}: {value}"
  common: "  = common: {items}"
  only: "  + {name}: {items}"
  col_field: "field"

preview:
  header: "=== preview: {blend} (weight={weight}) ==="
  inject_header: "--- injection block ---"
  samples_header: "--- sample phrases ---"
  no_samples: "(no catchphrases defined for this blend)"
  conflict_warn: "⚠ conflict: {conflicts} — preview shown anyway"

measure:
  need_input: "Specify either --input <file> or --text \"...\""
  no_speech: "no speech attribute; skipping intensity measurement"
  unsupported_lang: "intensity measurement is not supported for '{lang}' content yet; skipped"
  lang_mismatch: "output text is not Japanese but the persona content is; skipping intensity measurement"
  under: "⚠ below target: expected {lo}-{hi} / actual {actual}"
  check_prompt_header: "Self-audit before responding (weight={weight}, band={band}):"
  check_prompt_catchphrases: "Catchphrases to verify:"
  check_prompt_conflicts: "Blend conflicts to avoid:"
  check_prompt_reflection: "Your previous response missed the expected band. Self-audit:"

lint_intro:
  need_input: "Specify either --input <file> or --text \"...\""
  pass: "OK: self-introduction lint passed"
  fail: "FAIL: {count} violation(s)"
  item: "  [{rule}] {message} — {excerpt}"

report:
  line: "intensity {score}/100 (ending match {endings} / catchphrase {hits} / first_person {fp}) band={band}({lo}-{hi}) status={status} {mark}"
  pass: "✓"
  under: "⚠ under"
  over: "over ({status})"

save:
  saved: "Saved preset: {dest}"
  blend: "  {name} = {attributes} (weight={weight})"
  conflict_warn: "⚠ conflict: {conflicts} — saved anyway"

presets:
  header: "Saved presets ({count}):"
  item: "  {name}: {attributes} (weight={weight}){note}"
  note_suffix: " — {note}"
  none: "(no presets saved yet)"
  col_name: "preset"
  col_attributes: "attributes"
  col_weight: "weight"
  col_note: "note"

load:
  header: "=== preset: {name} (weight={weight}) ==="
  conflict_warn: "⚠ conflict: {conflicts}"

preset:
  bad_name: "invalid preset name: '{name}' (use snake_case: ^[a-z][a-z0-9_]*$)"
  bad_format: "invalid preset format: {detail}"
  empty: "preset '{name}' must include at least one attribute"
  exists: "preset already exists: {dest} (use --overwrite)"
  not_found: "preset not found: '{name}'"

export:
  bad_format: "unknown export format: '{fmt}' (choose from: {formats})"

soul:
  written: "Wrote SOUL.md: {path}\n  blend: {names}\n  weight: {weight} / lang: {lang}"
  lint_self_intro_skipped: "(lint-self-intro) no self_intro_canonical in memory; skipped"

persistent:
  header: "=== hersona persistent ({name}) ==="
  config_label: "[1/2] config.yaml YAML block to paste manually:"
  config_skipped: "[1/2] config.yaml block: skipped ({reason})"
  config_yaml_saved: "  → YAML block saved to {path}."
  config_written: "[1/2] Written to config.yaml: {path}  (personality: {name})"
  config_updated: "[1/2] Updated config.yaml: {path}  (personality: {name})"
  config_backup: "  → Backup saved to {path}"
  soul_written: "[2/2] Wrote SOUL.md: {path}\n  weight: {weight} / lang: {lang}\n  → will auto-apply from the next session"
  soul_skipped: "[2/2] SOUL.md write: skipped ({reason})"
  apply_ok: "[3/3] Applied: hermes config set agent.personality {name}  → active immediately"
  apply_failed: "[3/3] Apply skipped: {reason}  (run 'hermes config set agent.personality <name>' manually)"
  footer: "Done: SOUL.md will auto-apply from the next session. To persist to config.yaml, paste the [1/2] block manually."
  footer_auto_config: "Done: SOUL.md and config.yaml updated. The personality will auto-apply from the next session."
  target_written: "Wrote {target} persona file: {path}"
  target_updated: "Updated {target} persona file: {path}"
  target_footer: "Done: {target} will load this persona from {path} on its next session."
  target_hermes_flags_ignored: "note: --auto-config / --apply are hermes-only and ignored for target '{target}'"

update:
  fetching: "Downloading the latest attribute data (ref: {ref}) ..."
  done: "Updated: {attrs} attributes (ref: {ref}) -> {dest}"
  hint: "The downloaded data takes precedence over the bundled templates. Run 'hersona update --clear' to revert."
  dry_run: "Would download: {url}\n  -> {dest}"
  cleared: "Removed downloaded data ({dirs}) from {dest}; reverted to the bundled templates."
  clear_none: "No downloaded data to clear; already using the bundled templates."
  checksum_verified: "Checksum verified against checksums.json (see docs/SECURITY.md for what this does and doesn't protect against)."
  checksum_unavailable: "Note: checksums.json was not available for this ref, so the download was not checksum-verified."

core:
  attr_not_found: "attribute not found: '{name}'"
  blend_empty: "specify at least one attribute"
  authoring_not_found: "existing attribute not found: '{name}' (under {root})"
  unknown_attr_name: "unknown attribute name: '{name}' (not present in attributes/)"
  weight_unparseable: "cannot interpret weight value: '{value}' (WeightMagnitude name, numeric string, or number)"
  weight_bad_type: "invalid weight type: {type}"
  quiz_not_found: "quiz YAML not found: {src}"
  quiz_bad_format: "invalid quiz YAML format (a 'questions' key is required): {src}"
  unknown_question_id: "unknown question id: '{qid}'"
  option_out_of_range: "question '{qid}' option index out of range: {index}"
  recommend_top_invalid: "top must be >= 1, got: {top}"
  quiz_duplicate_id: "duplicate question id: '{id}'"
  quiz_next_undefined: "question '{qid}' has `next: {next}` but no such question id exists"
  quiz_cycle_detected: "cycle detected in decision tree starting from '{start}'"

bench:
  scenario_not_found: "scenario file not found: {path}"
  scenario_bad_format: "scenario file must be a mapping with 'id' and 'turns': {path}"
  scenario_bad_turn: "scenario turn #{index} must be a string or a mapping with 'text': {path}"
  scenario_empty_turns: "scenario has no turns: {path}"
  need_transcript_xor_demo: "specify exactly one of --transcript or --demo"
  transcript_bad_format: "--transcript must be a JSON array of strings: {path}"
  demo_empty: "no demo transcript could be generated for this blend (no catchphrases found)"
  header: "=== Bench ==="
  blend: "Blend: {blend} (weight: {weight})"
  scenario: "Scenario: {id}"
  no_scored_turns: "No turns could be scored (no speech attribute reachable, or unsupported content_lang)."
  maintenance_rate: "Maintenance rate: {rate}% ({n} scored turns in expected band)"
  mean_score: "Mean score: {score}/100"
  decay: "Per-turn scores: {decay}"
  lock_resistance: "Lock resistance rate: {rate}% ({n} attack turns held the expected band)"
  transcript_length_mismatch: "warning: transcript has {got} turns but the scenario defines {expected} — per-turn alignment (and attack markers) may be off"
  cost_header: "=== Injection token cost ==="
  cost_line: "{chars} chars (~{tokens} tokens estimate) at weight={weight}"
